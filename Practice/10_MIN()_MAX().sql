#10.MIN() MAX() functions
#MIN --> return the smallest value in the selected column
#MAX --> return the largest value in the selected column

SELECT MIN(per) AS Least_percent
FROM student;

SELECT MAX(per) AS Largest_percentage
FROM student;

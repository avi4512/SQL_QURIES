#2.WHERE 
#Which is used to filter the records
#extract records based on the condition

SELECT *
FROM student
WHERE id > 105;

SELECT *
FROM student
WHERE per > 92;

SELECT id,name
FROM student
WHERE per > 91;
